<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.labo.ingesup.series" >

    <!-- Permissions necessaires pour avoir accès à internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".application.SerieApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".activities.DetailActivity">


        </activity>
        <activity
            android:name=".activities.GenreActivity">

        </activity>


        <activity
            android:name=".activities.CreateSerieActivity">

        </activity>

        <activity
            android:name=".activities.SerieActivity">

        </activity>

        <activity
            android:name=".activities.YoutubeActivity">

        </activity>
        <activity
            android:name=".activities.AccueilActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
